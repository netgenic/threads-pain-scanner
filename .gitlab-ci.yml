image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp docs/privacy.html public/privacy.html
    - cp docs/privacy.html public/index.html
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
